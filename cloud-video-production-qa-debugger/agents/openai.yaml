interface:
  display_name: "Cloud Video Production QA Debugger"
  short_description: "Debug QA cloud video and direct COS uploads"
  default_prompt: "Use $cloud-video-production-qa-debugger to upload local media directly to QA COS and create a Cloud production without using production credentials."
